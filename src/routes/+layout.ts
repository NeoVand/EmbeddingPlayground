// The playground depends entirely on browser-only APIs: WebGPU, transformers.js,
// Three.js. There's nothing meaningful to render server-side.
export const ssr = false;
export const prerender = false;
